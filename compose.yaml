services:
  poe-companion-db:
    image: postgres:18.6
    environment:
      - POSTGRES_DB=poe-companion-db
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_USER=${POSTGRES_USER}
    ports:
      - "5432:5432"
